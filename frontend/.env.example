# Copy to .env.local for local overrides (optional — dev proxies /api by default).
# In production (Vercel), set this to your Render backend's API URL:
#   VITE_API_BASE=https://boobly-api.onrender.com/api
VITE_API_BASE=
